# Implementation Review — 2026-09-24 17:08 ET

**1 PASS / 0 FAIL.**

Window: **2026-09-24 13:22 ET → 2026-09-24 17:09 ET** (3h 47m), from `LAST-REVIEW.json`.

Three transcripts were touched inside the window, all in the `desire-confidant-craft` project
folder. All three were grepped case-insensitively for `process alert`, assistant turns included.

```
57dc3ff4  implementation review 13:08     its own run; window closed at 13:22, tail is report + LAST-REVIEW
0e404d51  producer-sweep       13:54 ET   ← the one claim
765fd6da  producer-sweep       15:54 ET   no process change; see UNFLAGGED
c6481281  this run
```

No interactive session with Glenn fired inside this window — no `.jsonl` outside the three above
was modified after 13:20, so there are no unrecorded verbal agreements to chase.

**One flagged claim, and it landed in full**, including both of the branches its author named as
proof-of-failure, which I exercised myself in this run. **The Process Alert convention held:
1 flagged / 3 unflagged, and none of the three unflagged items required an alert.**

---

## FAIL

**None.**

---

## PASS

### P1 — `post-ghl.js --platform fb|ig` · FLAGGED · 14:18 ET, session `0e404d51`

> *"**Process Alert: `post-ghl.js` gained `--platform fb|ig`, so a half-published row can be
> repaired without double-posting the half that landed.** It requires `--no-calendar` and refuses
> to combine with `--entry`; the caption guards now test the narrowed set. content-kit commit
> `65031d0`, pushed, level with origin/main."*

Every part verified in this run, independently of the author's own evidence.

**Committed and pushed** — `content-kit`, on `origin/main`:

```
$ git log --oneline -1
65031d0 2026-09-24 13:59:55 -0400  post-ghl: --platform fb|ig, so a half-published row can be repaired
$ git status -sb
## main...origin/main            (no ahead/behind)
$ git branch -a --contains 65031d0
* main
  remotes/origin/main
```

**The code is in the file, not only in the commit** — `post-ghl.js` narrows the send and the
guards read the narrowed set, so an FB-only repost is allowed a bare URL while the Instagram
"URLs aren't clickable" guard still fires whenever Instagram is in the send:

```
 94: const targetAccounts = !platform ? ACCTS[brand]
 95:   : ACCTS[brand].filter((id) => (platform === "ig") === IG_ACCOUNTS.has(id));
211:   const postingToIG = targetAccounts.some((id) => IG_ACCOUNTS.has(id));
212:   const postingToFB = targetAccounts.some((id) => !IG_ACCOUNTS.has(id));
298:     accountIds: targetAccounts,
```

**Proven by use — read back from GHL in THIS run**, not taken from the author's report. Memory
`ghl-swaps-post-ids-on-publish` says a published post can 404 on a `get`; this one did not, and
returned the whole record:

```
$ ghl-social-post {action:"get", postId:"6ab567a09a234aed09d95cee"}   http 200
  platform     facebook
  status       published
  error        null
  publishedAt  2026-09-24T18:10:55.124Z
  deleted      false
  accountIds   [ ..._987031901151211_page ]          ← ONE account: the PixFix FB page only
  previewLink  https://www.facebook.com/reel/2238329910046686/
  media        pixfix-2026-09-23-eight-angles-15s.mp4
```

The single `accountIds` entry is the proof the flag did its job — an unnarrowed send would carry
the PixFix Instagram account too, and would have double-posted the half that already landed.

**Proven it can fail — I ran both rejections just now**, exit codes captured without a pipe:

```
$ node post-ghl.js --brand pixfix --platform tiktok --no-calendar --caption nope.txt --media x
  --platform must be fb or ig                                            exit=1

$ node post-ghl.js --brand pixfix --platform fb --entry abc123 --caption nope.txt --media x
  --platform is for repairing a half-published row, which is already booked.
    Use it with --no-calendar so the row is not booked a second time.     exit=1
```

**Scope note, not a deduction.** The same session separately hand-patched the PixFix row's
`publish_results` and closed a spec on it at 14:17. The 15:54 sweep found the reconciler had
rebuilt that column from the original GHL post at 14:30 and erased the patch. **That regression is
outside this alert** — the alert claims the flag and the repair post, both of which stand — and it
is already written up (`73a1152`, below). The repair itself is live; only the bookkeeping reverted.

---

## CONTRADICTIONS

### C1 — Who builds video · **FIFTH consecutive cycle open, byte-for-byte unchanged**

Re-grepped every `scheduled-tasks/*/SKILL.md` against the charter in this run.

`CLAUDE.md:17–19`, restated at `:288`:

> *`peer-morning-run` takes reels, video, ads, episodes and VO — **nothing else can build video**.*

`producer-sweep/SKILL.md:32`, unchanged:

> **3. VIDEO SLOTS ARE YOURS TO PRODUCE.** … **If a video slot is within 2 hours, build and post it.**

`producer-sweep/SKILL.md:20` says the same thing a second time: *"IF IT NEEDS A REEL, YOU create
it."* `peer-morning-run/SKILL.md:6` and `builder-morning-run/SKILL.md:6` both open
`██ YOU DO NOT WRITE COPY. ██` and defer to the charter. **`producer-sweep` remains the only file
claiming a capability the charter reserves.**

The 21:08 review on 9/21 put a one-line recommendation to Glenn — carve the in-window rescue into
the charter, then trim `SKILL.md:32` to mechanics. **He has not answered, and I am not re-arguing
it.**
**Next action: Glenn says yes or no. The charter edit and the `SKILL.md` trim are then one commit.**

No other `SKILL.md` contradicts the charter.

---

## UNFLAGGED

Three changes, **none of which needed an alert** — two spec files and one ordinary render.
The convention is being used correctly.

### U1 — the FB re-connect instruction was withdrawn · 14:12 ET, `0e404d51`

The 11:54 spec asking Glenn to re-connect the RVA and PixFix Facebook pages was closed with the
re-connect struck through, on evidence both pages published FB Reels today. **Withdrawing a
standing instruction to Glenn is a documentation correction, not a machine change** — correctly no
alert. Committed and pushed: `content-build 2125917`, `main` level with `origin/main`.

### U2 — a queued fix was invalidated before it was built · 16:02 ET, `765fd6da`

`specs/platform-repair-writeback.md` proposed a `record_repair` writeback to stop the sweep
re-reporting a repaired row. The 15:54 sweep proved that cannot hold — the reconciler owns
`publish_results` and rebuilds it from the original GHL post every ~15 min, which is what erased the
14:17 hand-patch at 14:30. The spec now recommends making the reconciler aware of the repair post
id, a proof step that only counts after a reconcile pass, and `content_misfires` as the durable
record. **A proposal is not a machine change** — correctly no alert. `content-build 73a1152`,
pushed, `main` = `origin/main` = `73a11520…`. Also `35c85a3` (14:18), the original write-up.

### U3 — 9/26 11:00 UGH slot rendered · 15:57 ET, `765fd6da`

Copy was complete, image was missing; rendered through the producer, QA 9/9, row `ready`,
`rendered_at 19:57:58Z`. **Ordinary production work under the charter's "no alert for building and
booking a post."** No copy was authored — the caption was already there.

---

## STANDING DEFECT, UNCHANGED

**`ugh-charter-review-3x-daily` is still frozen — still 8 missed fires, no new one this window.**
Verified live in this run:

```
taskId      ugh-charter-review-3x-daily     enabled true
cron        30 4,11,19 * * *
lastRunAt   2026-09-22T01:38:10Z   (2026-09-21 21:38 ET)
nextRunAt   2026-09-24T23:37:19Z   (2026-09-24 19:37 ET)
```

Count is unchanged from the 13:08 review because today's 19:30 fire has not come round yet. The
next one is tonight at 19:37 ET and **it is the test**: memory `scheduled-task-permission-freeze` —
an enabled task advances `nextRunAt` while never running, so `nextRunAt` proves nothing.
**Next action: read `lastRunAt` again after 19:37 ET tonight. If it has not moved, the task needs
deleting and recreating, not enabling.**

Every other scheduled task has a `lastRunAt` inside the last 24 hours. `producer-sweep` fired both
of its in-window slots (`17:54Z` and `19:54Z`).

---

## NOT CHECKED

- **The two story-poll `SKILL.md` folders** (`rva-story-poll-friday`, `rva-story-poll-monday`)
  still sit in `.claude/scheduled-tasks/` with no registered task behind them. Raised on 9/21 and
  twice on 9/22, unchanged, and no work in this window touched them. Not re-verified line by line
  this run — only that `list_scheduled_tasks` still returns no task by either name.
- **The Facebook reel URLs signed out.** I fetched both (`HTTP 200`) but the login wall strips the
  `og:` tags, and a bogus reel id returns the same 200 with the same stripped body — so the fetch
  distinguishes nothing and is not evidence either way. The GHL read-back above is the evidence
  instead.
- **The 9/21 RVA partial.** Left deliberately by the 15:54 sweep because its caption column is
  empty (`cap_len 0`, `assigned_to peer`) and posting it would mean authoring copy. That is the
  charter's lane rule working, not a defect. I did not re-derive its state.
