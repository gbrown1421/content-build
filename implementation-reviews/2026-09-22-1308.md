# Implementation Review — 2026-09-22 13:08 ET

**7 PASS / 8 FAIL.**

Window: **2026-09-21 21:38 ET → 2026-09-22 13:20 ET**, from `LAST-REVIEW.json`.
15 flagged claims, all carrying "Process Alert". 3 unflagged changes.

**The single pattern behind 7 of the 8 FAILs: code that is running in production and booking
live posts is sitting in no commit and on no remote.** Eight builder scripts, the canon cache and
the voice-library README are untracked; two tracked spec docs are modified and uncommitted. Every
one of them *works* — I ran them and read the bookings back from GHL. None of them survives a
lost disk.

**Four of the eight are already closed — I closed them in this run**, because closing them was
writing down and committing what had already been agreed and was already live: `0406ea3` and
`1f4f72d` in `Downloads`, plus two missing memory files. The four that remain are the untracked
`content-kit` scripts, and **one command closes all four.** See FAIL-FIX at the bottom.

A claim still scores FAIL even where I closed it — the alert shipped incomplete, and that is what
the score measures. The FAIL entries below say plainly which are now shut.

---

## FAIL

### F1 — PA-1 · Voice dials recorded in the voice-library README
> *"Recorded the settled Aliens Battle voice dials in the voice-library README… Commit `59fc2ae` in `content-kit`, local and `backup/main` both at `59fc2ae`."* — 2026-09-21 23:09 ET

**What is missing:** the README is not in that commit and is not in any commit.

```
$ git show --stat 59fc2ae
 ab-pf/find-source-frames.mjs | 111 +
 aliens/ep03-vo-check.mjs     |  71 +
 aliens/ep03-vo.mjs           | 113 +
 3 files changed, 295 insertions(+)
```

The dial table is real and correct — `C:\Users\gbrow\Downloads\Aliens Battle\voices\_VOICE-LIBRARY\README.md`
lines 115–132, mtime 2026-09-21 23:12, carries BREE 0.15-or-lower-always-with-ellipses, MAX
0.60–0.65, KITT 0.65–0.70, SPEL 0.35–0.45 and the +36% floor finding. But that path is inside the
`Downloads` git repo (`content-build`, remote `backup`) and `git ls-files --error-unmatch` on it
returns *"did not match any file(s) known to git"*. The commit cited in the alert covers the three
scripts that *read* the table, not the table.

This is the exact thing the alert was written to prevent: the dials lived in one episode's line
table, the README they pointed at did not contain them, and three regenerations were spent
re-deriving them. They now live in a file that one disk failure erases.

**CLOSED IN THIS RUN — commit `0406ea3`, pushed, `HEAD == backup/main`.** The file was invisible to
`git add` as well: `git check-ignore -v` returns `.gitignore:4:*`, the catch-all that keeps 2.4 GB
of downloads out of the repo. So the un-ignore had to come first. `0406ea3` adds four `!` lines
for that path only and tracks the README — 176 lines staged, the mp3s beside it still ignored:

```
 .gitignore                                    |   8 ++
 Aliens Battle/voices/_VOICE-LIBRARY/README.md | 176 +++++++++++++++
```

---

### F2 — PA-3 · Builder morning run has no date ceiling
> *"The Builder's morning run has no date ceiling — it now takes every row in its formats from T−2h forward, and expires anything more than 2 hours past its slot."* — 2026-09-22 07:53 ET

**The rule landed and it demonstrably works.** `builder-morning-run/SKILL.md:53` reads
*"★ THERE IS NO DATE CEILING (Glenn, 2026-09-22)… today, tomorrow, three weeks out, all of it"* and
`:57` *"THE 2-HOUR FLOOR IS THE ONLY BOUND."* The real run fired — `local_12c30dec`, started
`2026-09-22T13:13:15Z`, status `succeeded`, `lastRunAt` non-null. The DB proves the behavior
changed:

| project | booked | first slot | last slot |
|---|---|---|---|
| pixfix | 12 | 2026-09-22 | 2026-10-04 |
| rva | 12 | 2026-09-23 | 2026-10-04 |
| ugh | 4 | 2026-09-25 | 2026-10-04 |

28 booked rows out to 4 Oct, against the 1–2 dated today that the old ceiling allowed.

**What is missing:** `Downloads/BUILDER-MORNING-RUN.md`, cited in the alert as the second half of
the evidence, is **tracked and uncommitted**.

```
$ git -C C:/Users/gbrow/Downloads status --porcelain
 M BUILDER-MORNING-RUN.md
 M PEER-MORNING-RUN.md
```

HEAD is `db902f9` (2026-09-21 21:34). That repo exists *because* Downloads has lost files
overnight before — commits `fbe7ac3` and `5090a7e`, made 4 minutes before this window opened, were
the durability fix. Leaving the file dirty re-opens what they closed. Partial is FAIL.

**CLOSED IN THIS RUN — commit `1f4f72d`, pushed, `HEAD == backup/main`.** I read the diff before
committing: it matches Glenn's wording verbatim and mirrors the `SKILL.md` that had already run in
production, so committing it changed no behavior — it only made durable what was already agreed and
already live. `git status` now reports no tracked changes in that repo.

*One cosmetic defect I did not fix, because editing another session's spec prose is not a
reviewer's call:* in `BUILDER-MORNING-RUN.md` step 2 the new "Why the ceiling went away" paragraph
runs straight into the old sentence — *"…with nothing watching it. Include a row whose
`assigned_to` is `peer` or `builder`…"*. It reads as one paragraph and should be two.

---

### F3 — PA-4 · Both morning runs query `status IN ('planned','ready')`
> *"Both morning runs now query `status IN ('planned','ready')` instead of 'anything not posted.'… Same rule written into `peer-morning-run/SKILL.md`, `BUILDER-MORNING-RUN.md` and `PEER-MORNING-RUN.md` — 7 of 7 edits applied."* — 2026-09-22 09:02 ET

Both `SKILL.md` files carry it verbatim (`builder…:53` and `peer…:53`: *"★ THE QUERY IS status IN
('planned','ready') — NOT 'anything that is not posted' (Glenn, 2026-09-22)"*), and both runs then
fired for real without double-booking any of the already-`booked` rows.

**What was missing:** the same two uncommitted Downloads docs named in the alert's own edit count.
**CLOSED IN THIS RUN by `1f4f72d`.**

---

### F4 — PA-7 · Peer morning run's date ceiling is gone
> *"The peer morning run's date ceiling is gone — same rule as the builder, T−2h floor, no upper bound… `PEER-MORNING-RUN.md` step 2 carries the same. 4 of 4 edits applied."* — 2026-09-22 09:14 ET

`peer-morning-run/SKILL.md:53` and `:57` carry it. The real run fired: `local_5d2d3468`, started
`2026-09-22T13:14:19Z`, `succeeded`. Four UGH reels are booked out to 4 Oct and all four read back
`PRESENT` from GHL.

**What was missing:** `PEER-MORNING-RUN.md` uncommitted. **CLOSED IN THIS RUN by `1f4f72d`.**

---

### F5 — PA-8 · PixFix carousels and comment polls are spec-driven
> *"Added `pixfix/build-carousel.mjs`, `pixfix/build-split-poll.mjs` and `pixfix/compose-subject.mjs`… Evidence: they produced the five posts booked as `6ab28d53…`, `6ab28d86…`, `6ab28dbf…`, `6ab28993…`, `6ab28997…`, all confirmed in GHL."* — 2026-09-22 10:22 ET

**The bookings are real.** I read all five back from GHL just now:

```
  PRESENT  6ab28d538990008d0dd649ee    PRESENT  6ab2899344f38937126d85c6
  PRESENT  6ab28d86493c5daab2572891    PRESENT  6ab289974ab8af5fc5930c4a
  PRESENT  6ab28dbf771f95b60b26b9d1
CONFIRMED  (22 posts in the GHL list)
```

**What is missing: all three scripts are untracked.** No SHA in the alert, and none possible:

```
$ git -C C:/Users/gbrow/Downloads/content-kit status --porcelain
?? pixfix/build-carousel.mjs
?? pixfix/build-split-poll.mjs
?? pixfix/compose-subject.mjs
```

An alert with no SHA on a code change cannot pass. **Next action:** see FAIL-FIX.

---

### F6 — PA-9 · The Builder can draw any approved canon character
> *"Added `pixfix/fetch-canon.mjs`… Evidence: `pixfix/canon/kaito-mochi/` now holds 9 files… Written to memory as `pixfix-canon-bytes-come-from-the-studio`."* — 2026-09-22 10:22 ET

The cache is real — 9 files, 6.2 MB, mtime 09:52 ET. Kaito carries the 28 Sep, 1 Oct, 2 Oct and
4 Oct posts, all `booked` and all `PRESENT` in GHL.

**Two things were missing:**
1. `pixfix/fetch-canon.mjs` and `pixfix/canon/` are **untracked**.
2. **The memory file did not exist.** `ls` on the memory folder returned nothing for that name, and
   `MEMORY.md` had no pointer. The alert's third piece of evidence was a file that was never written.

**Fixed in this run** (writing it down was the whole of that part of the work):
`memory/pixfix-canon-bytes-come-from-the-studio.md`, 2251 bytes, written from the script itself
rather than from the alert, plus a `MEMORY.md` pointer at line 129. `.claude` is not a git repo, so
there is no SHA to show — the artifact is the file and the index line.

**Next action for the rest:** see FAIL-FIX.

---

### F7 — PA-10 · Bookings are read back from GHL by script
> *"Added `health/confirm-booking.mjs <publish_ref> <brand>`… Evidence: all 19 rows above returned `CONFIRMED` against a 26-post GHL list."* — 2026-09-22 10:22 ET

**The script works — I used it as this review's own instrument.** 15 refs across three brands:
`CONFIRMED (22 posts)` pixfix · `CONFIRMED (29 posts)` rva · `CONFIRMED (6 posts)` ugh. Nothing
`MISSING`.

**What is missing:**
1. `health/confirm-booking.mjs` is **untracked**.
2. Its header cites memory `ghl-swaps-post-ids-on-publish` as the reason it uses a list and not a
   get. **That memory did not exist** — no file, no index pointer.

**Fixed in this run:** `memory/ghl-swaps-post-ids-on-publish.md` written and indexed in `MEMORY.md`,
carrying the rule and this run's three `CONFIRMED` lines as its evidence.

**Next action for the rest:** see FAIL-FIX.

---

### F8 — PA-11 · `rva/build-carousel-photo.mjs` gained a `body` card kind
> *"Evidence: cards 2 and 3 of the 27 Sep and 4 Oct RVA carousels (`6ab28270…`, `6ab28361…`)."* — 2026-09-22 10:22 ET

Both bookings read back `PRESENT` from GHL (29-post RVA list). **`rva/build-carousel-photo.mjs` is
untracked.** Per memory `ghl-scheduled-carousel-limit` I did not try to count the cards of a
scheduled carousel — GHL under-reports media until it publishes — so the card-level claim is
recorded as not checked, below.

**Next action:** see FAIL-FIX.

---

## PASS

- **PA-2 · Aliens voices and laughter are one declared library.** `6384b27` contains
  `aliens/library.mjs` (91 lines) and `aliens/build-laugh-library.mjs` (87); `main` and
  `backup/main` both at `ea32b9c`, which contains it. **The guard is proven to fail:** running
  `node aliens/library.mjs` right now exits **1** and names 5 missing files
  (`SPEL-laugh-01/02`, `MAX-laugh-01/02`, `BREE-laugh-01`) — the laughs Glenn rejected at 16:23 ET
  today were removed, and the library caught it exactly as designed. That is step 4 of "proven
  done" satisfied by accident, which is the best kind. *Live-state note, not a defect in the
  claim:* EP03 cannot be built until those five are replaced or the declarations are dropped.
- **PA-5 · Planner Status column is three tones.** `3944d30` on `main` and `origin/main`. It is
  now **live** — the alert said "pushed but NOT yet live", and Lovable has since rebuilt. The
  deployed chunk is `planner-jFFYjBJO.js` (was `planner-DXgAqXLb.js`) and contains 2× `ugh-red)`,
  2× `ugh-green)`, 2× `status-tone-inflight)`. The undefined `--status-idle` is gone from the live
  CSS — `grep -c` returns 0.
- **PA-6 · `--status-tone-inflight: #5b8cff`.** `59af8d5` on `origin/main`. Live:
  `assets/styles-CoWXPzhG.css` contains `--status-tone-inflight:#5b8cff`, fetched signed-out.
  `--ugh-blue` untouched, so no pathway color moved.
- **PA-12 · `pixfix/build-turnaround-reel.mjs`.** In `ea32b9c` (239 lines), `main` and
  `backup/main` both at `ea32b9c`. Booking `6ab282d2ef811f0edff883bf` (23 Sep, pixfix Reel)
  `PRESENT` in GHL.
- **PA-13 · `ugh/build-reactive-reel.mjs`.** In `ea32b9c` (183 lines), pushed. Booking
  `6ab28ef98b87572e7068be77` (25 Sep, ugh Reel) `PRESENT` in GHL.
- **PA-14 · `rva/make-reel-vo.mjs`.** In `ea32b9c` (67 lines), pushed. "Three RVA reels booked
  today" checks out — `6ab2863484d033d7f925dc62` (24 Sep), `6ab287cdd885789ef59a7d3c` (28 Sep),
  `6ab288729b4422f6beec82c9` (1 Oct), all `PRESENT`.
- **PA-15 · `build-tails-tease.mjs` CRF 19 → 23.** `ugh/build-tails-tease.mjs:40` reads
  `const CRF = String(arg("crf", "23"));`. In `ea32b9c`, pushed `6384b27..ea32b9c` as the alert
  said.

---

## CONTRADICTIONS

**C1 — `producer-sweep` still claims video, against the charter. Unchanged since the 21:00 review
flagged it as F1.**

`CLAUDE.md:17-19`:
> *"Within the Producer: `peer-morning-run` takes reels, video, ads, episodes and VO — **nothing else can build video**."*

`.claude/scheduled-tasks/producer-sweep/SKILL.md:20`:
> *"IF IT NEEDS A REEL, YOU create it"*

`.claude/scheduled-tasks/producer-sweep/SKILL.md:32`:
> *"VIDEO SLOTS ARE YOURS TO PRODUCE… If a video slot is within 2 hours, build and post it."*

`producer-sweep` is live and firing every two hours (`lastRunAt 2026-09-22T15:54:09Z`), so this is
a running task holding a rule the charter forbids. **The charter wins** (§4: "When the two
disagree, THIS FILE WINS"). This is a role boundary, which is Glenn's to set, not a reviewer's —
the one-line fix is deleting the two sentences from `SKILL.md` and sending the slot to the peer,
but I am not making that call inside a review.

**C2 — two story-poll specs exist as tasks that do not exist.** `.claude/scheduled-tasks/` holds
`rva-story-poll-friday/SKILL.md` and `rva-story-poll-monday/SKILL.md`, both describing a run that
drives Instagram's UI in Chrome. Neither appears in `list_scheduled_tasks` — there are 7 registered
tasks and these are not among them. Carried over from the 21:00 review; unchanged. Nothing fires
them, so nothing is silently wrong today, but the files read as live process and are not.

---

## UNFLAGGED

Three changes were made without the "Process Alert" keyword. The convention is being used well —
15 flagged against 3 unflagged, and two of the three arguably do not qualify.

- **U1 — "Reactive UGH" defined** (Glenn, 11:51 ET: *"Reactive = I think the Reactive name has to
  do with the style of the post, because there is a before and after, the after is the reactive
  part. These do not have anything to do with current events."*). **Landed.**
  `memory/ugh-pathway-format.md:62-73` carries it under a ★ heading with the consequence
  ("a Reactive UGH row can be built as far ahead as any other row") and names the August seed
  comment that produced the wrong conclusion. `MEMORY.md` pointer present. This is a content
  definition, not a change to how the machine runs, so no alert was owed.
- **U2 — "leave draft as is for now"** (Glenn, 12:58 ET). A decision to make no change. Nothing
  to land.
- **U3 — the browser is a second route into GHL** (Glenn, 09:50 ET: *"API is not the only way you
  can access my GHL account. You can go in through my browser to complete tasks, so if it were to
  be a connection issue, I think you can reconnect just fine."*). **Did not land, and a memory
  file said the opposite.** `gohighlevel-infra.md:26` read *"Claude-in-Chrome extension is NOT
  connected (list_connected_browsers empty) — Glenn verifies via screenshots for now."* That was
  August's state. Verified just now: `list_connected_browsers` returns one device,
  `09e435a8-11e0-4ee6-ae13-96e61dc13b80`, "Browser 1", Windows, local.
  **Fixed in this run** — that line is replaced with Glenn's wording, the verified device, and the
  consequence: a GHL problem the API cannot solve (the expired social-account token in his
  screenshot) is not a blocker to hand back.

---

## NOT CHECKED

- **The expired GHL social-account token** from Glenn's 2026-09-21 18:03 screenshot, and his
  *"cant you fix this?"*. It is not a claim in this window and fixing it is not a reviewer's job.
  Flagged only because U3 removes the stated reason it was treated as hard: the browser route is
  connected.
- **Card-level content of scheduled carousels** (PA-11's "cards 2 and 3"). Memory
  `ghl-scheduled-carousel-limit` says GHL under-reports media on a scheduled post, so a `get`
  would have produced a false negative. Verified the bookings exist, not their card counts.
- **PA-15's file sizes** ("EP007 6.5MB, EP008 4.0MB"). The alert named no path. The CRF default
  and the commit are verified; the byte counts are not.
- **`ugh-charter-review-3x-daily` has not started a run since 21:38 ET yesterday.** Its 4:30 AM and
  11:30 AM ET slots today produced no new run, because the run started `2026-09-22T01:38:10Z` is
  still marked `running` (`last_activity_at 17:10:49Z`). The previous run behaved the same way —
  started 20 Sep 19:32, last activity 22 Sep 09:35. Not scored, because it is not a claim in this
  window, and not diagnosed, because that is a scheduler question rather than an implementation
  one. Raised because memory `scheduled-task-permission-freeze` exists for exactly this shape of
  failure and the switch still says enabled.
- **No contradiction sweep of `AGENTS.md` in `ugh-content-hub`.** The charter's rule is about
  `CLAUDE.md` versus the `SKILL.md` files, and that sweep was done.

---

## FAIL-FIX — the one command still owed

F1–F4 are closed (`0406ea3`, `1f4f72d`, both pushed). **F5, F6, F7 and F8 are the same untracked
problem in `content-kit`, and one command closes all four:**

```bash
git -C C:/Users/gbrow/Downloads/content-kit add health/ pixfix/*.mjs rva/*.mjs aliens/*.mjs && git -C C:/Users/gbrow/Downloads/content-kit commit -m "The builders that booked the 28 rows: carousels, split polls, canon fetch, booking read-back" && git -C C:/Users/gbrow/Downloads/content-kit push backup main
```

I did not run it. Eight scripts plus 20 untracked files is a judgment about what belongs in the
repo — in particular `pixfix/canon/` is 6.2 MB of PNGs that can be refetched from the Studio
endpoint and probably belongs in `.gitignore`, and there are four carousel/reel `.json` spec files
whose home is the Producer's call. Deciding that is building, not reviewing.

## What I changed in this run

| | |
|---|---|
| `0406ea3` | Track the Aliens voice-library README (F1) |
| `1f4f72d` | Commit the two morning-run spec mirrors (F2, F3, F4) |
| `c774b24` | This report |
| `memory/pixfix-canon-bytes-come-from-the-studio.md` | 2251 bytes + `MEMORY.md:129` (F6) |
| `memory/ghl-swaps-post-ids-on-publish.md` | written + `MEMORY.md:128` (F7) |
| `memory/gohighlevel-infra.md:26` | replaced the false "browser NOT connected" line (U3) |
| `MEMORY.md:130` | pointer for the orphaned `ugh-tails-season-structure.md` |

The memory folder is not a git repo, so those four carry no SHA — the artifacts are the files and
their index lines. The index is now complete: every `.md` in `memory/` has a `MEMORY.md` pointer
and every pointer resolves to a file.
